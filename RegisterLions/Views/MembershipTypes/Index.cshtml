@model IEnumerable<RegisterLions.Models.MembershipType>

@{
    ViewBag.Title = "ข้อมูลประเภทสมาชิก";
}



<div class="panel panel-primary">
    <div class="panel-heading">@ViewBag.Title</div>
    <div class="panel-body">
        <div class="col-md-6">
            <a class="btn btn-primary" href="@Url.Action("Create", "MembershipTypes")"><i class="glyphicon glyphicon-file"></i> เพิ่ม</a>
        </div>
        @using (Html.BeginForm("Index", "MembershipTypes", FormMethod.Post, new { name = "searchForm" }))
        {

            <div class="col-xs-10 col-lg-6 col-md-6">
                <div class="input-group">
                    <input type="text" id="searchString" name="searchString" class="form-control pull-right" placeholder="ค้นหา...">
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="button" name="submitButton" onclick="document.searchForm.submit();">
                            <i class="text-muted glyphicon glyphicon-search"></i>
                        </button>
                    </span>
                </div><!-- /input-group -->
            </div><!-- /.col-lg-6 -->
        }
        <table class="table table-bordered table-striped" id="myTable">
            <thead>
                <tr>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.membership_desc_thai)
                    </th>
                    <th class="text-center">
                        @Html.DisplayNameFor(model => model.membership_desc_eng)
                    </th>
                    <th class="text-center">แก้ไข</th>
                    <th class="text-center">ลบ</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.membership_desc_thai)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.membership_desc_eng)
                        </td>
                        <td align="center">
                            <a class="btn btn-success" href="@Url.Action("Edit", "MembershipTypes", new { id = item.membership_type})">
                                <i class="glyphicon glyphicon-edit"></i>
                            </a>
                            </td>
                        <td align="center">
                            <a class="btn btn-danger" href="@Url.Action("Delete", "MembershipTypes", new { id = item.membership_type })">
                                <i class="glyphicon glyphicon-trash"></i>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">&nbsp;</div>
</div>
