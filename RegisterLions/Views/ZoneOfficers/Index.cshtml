@model IEnumerable<RegisterLions.Models.ZoneOfficer>

@{
    ViewBag.Title = "บันทึกชื่อประธานเขต";
}

<div class="panel panel-primary">
    <div class="panel-heading">@ViewBag.Title</div>
    <div class="panel-body">
        <div class="col-md-6">
            <a class="btn btn-primary" href="@Url.Action("Create", "ZoneOfficers")"><i class="glyphicon glyphicon-file"></i> เพิ่ม</a>
        </div>
        @using (Html.BeginForm("Index", "ZoneOfficers", FormMethod.Post, new { name = "searchForm" }))
        {

            <div class="col-xs-10 col-lg-6 col-md-6">
                <div class="input-group">
                    <input type="text" id="searchString" name="searchString" class="form-control pull-right" placeholder="ค้นหา...">
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="button" name="submitButton" onclick="document.searchForm.submit();">
                            <i class="text-muted glyphicon glyphicon-search"></i>
                        </button>
                    </span>
                </div><!-- /input-group -->
            </div><!-- /.col-lg-6 -->
        }
        <table class="table table-bordered table-striped table-hover" id="myTable" cellspacing="0" width="100%">
            <thead>
                <tr>
                   
                    <th>
                        @Html.DisplayNameFor(model => model.fiscal_year_disp)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.zone_no)
                    </th>
                    <th>
                        ประธานเขต
                    </th>
                   
                    <th>
                        ประธานภูมิภาค
                    </th>
                    <th class="text-center">แก้ไข</th>
                    <th class="text-center">ลบ</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        
                        <td>
                            @Html.DisplayFor(modelItem => item.fiscal_year_disp)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.zone_no)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Member.full_name)
                        </td>
                        
                        <td>
                            @Html.DisplayFor(modelItem => item.RegionOfficer.Member.full_name) ภูมิภาคที่ @Html.DisplayFor(modelItem => item.RegionOfficer.region_no)
                        </td>
                        <td align="center">
                            <a class="btn btn-success" href="@Url.Action("Edit", "ZoneOfficers", new { id = item.zone_officer_id })">
                                <i class="glyphicon glyphicon-edit"></i>
                            </a>
                        </td>
                        <td align="center">
                            <a class="btn btn-danger" href="@Url.Action("Delete", "ZoneOfficers", new { id = item.zone_officer_id })">
                                <i class="glyphicon glyphicon-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
    <div class="panel-footer">&nbsp;</div>
</div>
